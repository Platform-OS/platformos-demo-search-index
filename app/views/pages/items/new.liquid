{% liquid

  unless context.session.authorized
    redirect_to '/sessions/new'
    break
  endunless

  assign categories = 'alpha,beta,gamma,delta' | split: ','
  render 'items/new', categories: categories
%}
